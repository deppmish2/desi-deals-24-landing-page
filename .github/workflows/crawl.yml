name: Daily Crawl

on:
  schedule:
    - cron: "0 6 * * *"
    - cron: "0 7 * * *"
  workflow_dispatch:

concurrency:
  group: daily-crawl
  cancel-in-progress: false

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Decide Berlin crawl window
        id: gate
        shell: bash
        run: |
          berlin_hour="$(TZ=Europe/Berlin date +%H)"
          berlin_date="$(TZ=Europe/Berlin date +%F)"
          echo "berlin_hour=$berlin_hour" >> "$GITHUB_OUTPUT"
          echo "berlin_date=$berlin_date" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || [ "$berlin_hour" = "08" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip outside Berlin window
        if: steps.gate.outputs.should_run != 'true'
        run: |
          echo "Skipping crawl."
          echo "Berlin date: ${{ steps.gate.outputs.berlin_date }}"
          echo "Berlin hour: ${{ steps.gate.outputs.berlin_hour }}"

      - name: Install dependencies
        if: steps.gate.outputs.should_run == 'true'
        run: npm ci

      - name: Run full daily crawl into Turso
        if: steps.gate.outputs.should_run == 'true'
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
          DESI_DEALS_DB_TURSO_DATABASE_URL: ${{ secrets.DESI_DEALS_DB_TURSO_DATABASE_URL }}
          DESI_DEALS_DB_TURSO_AUTH_TOKEN: ${{ secrets.DESI_DEALS_DB_TURSO_AUTH_TOKEN }}
          DB_BOOTSTRAP_ON_STARTUP: "true"
        run: npm run crawl

      - name: Warm public deals cache
        if: steps.gate.outputs.should_run == 'true'
        env:
          CLIENT_APP_URL: ${{ secrets.CLIENT_APP_URL }}
        shell: bash
        run: |
          base_url="${CLIENT_APP_URL%/}"
          if [ -z "$base_url" ]; then
            echo "Skipping warm step: CLIENT_APP_URL not configured."
            exit 0
          fi

          today_key="$(TZ=Europe/Berlin date +%F)"
          curl --fail --silent --show-error "$base_url/api/v1/deals?limit=20&page=1&in_stock=1&cache_date=$today_key" >/dev/null
